// Minimal reportWebVitals implementation to avoid undefined call
function reportWebVitals(metric) {
  if (metric && typeof console !== 'undefined') {
    console.log(metric);
  }
}

export default reportWebVitals;
